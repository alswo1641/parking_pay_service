name: parking_pay_service
description: 주차장 결제 서비스입니다.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6 #아이콘1
  flutter_riverpod: ^2.4.9 #리버팟
  permission_handler: ^11.1.0 #권한 설정
  logger: 2.0.2+1 #로거
  go_router: ^13.0.1 #라우터
  flutter_secure_storage: #로컬 스토리지 (보안 o)
  shared_preferences: ^2.2.2 #스토리지 저장 (보안 x)
  http: ^1.1.2 #http 통신 : dart 3.2 required
  material_design_icons_flutter: 7.0.7296 #아이콘2
  fluttertoast: ^8.2.4 #토스트메세지
  intl: ^0.19.0 #국제
  freezed: #직렬화
  freezed_annotation:
  flutter_naver_map: ^1.1.1 #네이버맵
  geolocator: ^10.1.0 #현재위치
  url_launcher: ^6.2.3 #앱열기
  xml: ^6.5.0
  json_annotation: ^4.8.1



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  retrofit_generator: '>=7.0.0 <8.0.0'
  build_runner: '>=2.3.0 <4.0.0'
  json_serializable: ^6.6.2



flutter:
   assets:
     - assets/images/
     - assets/icons/

uses-material-design: true
